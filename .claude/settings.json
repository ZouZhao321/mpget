{
  "permissions": {
    "allow": [
      "npm install",
      "npm run dev",
      "git status",
      "git log --oneline -10",
      "npx vitest run",
      "pnpm install",
      "pnpm run dev",
      "pnpm build",
      "pnpm add",
      "pnpm remove",
      "pnpm update"
    ],
    "deny": ["git push --force *"]
  }
}
